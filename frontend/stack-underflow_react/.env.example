# StackUnderflow React Frontend Environment Variables
# Copy this file to .env.local and modify as needed

# API Mode: mock, fake, or real
VITE_API_MODE=real

# API Base URL (used only in real mode)
VITE_API_URL=http://localhost:8080

# =============================================================================
# Mode Descriptions:
# =============================================================================
# mock  - Uses in-memory mock data (no backend required)
# fake  - Uses fake data generation (requires faker library)
# real  - Uses real backend API (requires running backend server)
# =============================================================================
